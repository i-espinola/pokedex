import React from 'react';
import Logotype from '../Brand';
import HeaderStyle from '../../assets/styled/components/HeaderStyled';

const Header = () => {
  return (
    <HeaderStyle>
      <Logotype />
    </HeaderStyle>
  );
};

export default Header;
